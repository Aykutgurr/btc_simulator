# Sunum oluşturucu

`generate.js` — PptxGenJS ile BTC Simulator sunum dosyası üretir.

```bash
cd tools/presentation
npm init -y
npm install pptxgenjs
node generate.js
```

Çıktı: proje kökünde `.pptx` dosyası oluşturulur (gitignore'da).
